

## Feasibility note

The Import Schedule preview uses a local sample extraction state and keeps manual entry available as the MVP fallback. **Parsing and OAuth are finalist-phase builds**; Google Calendar import is labelled Future build and is not enabled in this prototype.
